# blocking subpackage — enforcement mechanisms: hosts-file website blocking
# (hosts_blocker), distraction-process termination (app_killer), and the UAC
# elevation helper (admin) that hosts editing requires on Windows.
