# deepwork package — Windows productivity enforcement app.
# Modules are organized by feature (blocking/, monitoring/, feedback/, webui/)
# with cross-cutting config, state, storage, scheduler and logging at the top
# level; main.py at the repo root wires them together as named phases.
