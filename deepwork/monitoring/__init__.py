# monitoring subpackage — the capture→stitch→analyze pipeline: per-monitor
# screenshots (screen_capture), webcam photo (webcam_capture), labeled
# composite image (stitcher), and the OpenAI vision verdict (analyzer).
